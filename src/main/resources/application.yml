# Server settings
server:
  port: 8080

# Database configuration
# Replace the values with your actual database configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/emp_crud
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true